kind: Deployment
apiVersion: apps/v1
metadata:
  name: sc-web-deployment
  namespace: prod-stablecog
  labels:
    app: sc-web
spec:
  replicas: 4
  selector:
    matchLabels:
      app: sc-web
  template:
    metadata:
      labels:
        app: sc-web
    spec:
      containers:
        - name: sc-web
          image: replaceme
          ports:
            - containerPort: 3000
          imagePullPolicy: 'Always'
          resources:
            requests:
              memory: '2048Mi'
              cpu: '1000m'
            limits:
              memory: '3072Mi'
              cpu: '3000m'
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 10
          envFrom:
            - secretRef:
                name: sc-web-secrets
