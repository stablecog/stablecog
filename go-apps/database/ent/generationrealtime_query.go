// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yekta/stablecog/go-apps/database/ent/generationrealtime"
	"github.com/yekta/stablecog/go-apps/database/ent/predicate"
)

// GenerationRealtimeQuery is the builder for querying GenerationRealtime entities.
type GenerationRealtimeQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	inters     []Interceptor
	predicates []predicate.GenerationRealtime
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GenerationRealtimeQuery builder.
func (grq *GenerationRealtimeQuery) Where(ps ...predicate.GenerationRealtime) *GenerationRealtimeQuery {
	grq.predicates = append(grq.predicates, ps...)
	return grq
}

// Limit the number of records to be returned by this query.
func (grq *GenerationRealtimeQuery) Limit(limit int) *GenerationRealtimeQuery {
	grq.limit = &limit
	return grq
}

// Offset to start from.
func (grq *GenerationRealtimeQuery) Offset(offset int) *GenerationRealtimeQuery {
	grq.offset = &offset
	return grq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (grq *GenerationRealtimeQuery) Unique(unique bool) *GenerationRealtimeQuery {
	grq.unique = &unique
	return grq
}

// Order specifies how the records should be ordered.
func (grq *GenerationRealtimeQuery) Order(o ...OrderFunc) *GenerationRealtimeQuery {
	grq.order = append(grq.order, o...)
	return grq
}

// First returns the first GenerationRealtime entity from the query.
// Returns a *NotFoundError when no GenerationRealtime was found.
func (grq *GenerationRealtimeQuery) First(ctx context.Context) (*GenerationRealtime, error) {
	nodes, err := grq.Limit(1).All(newQueryContext(ctx, TypeGenerationRealtime, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{generationrealtime.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) FirstX(ctx context.Context) *GenerationRealtime {
	node, err := grq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GenerationRealtime ID from the query.
// Returns a *NotFoundError when no GenerationRealtime ID was found.
func (grq *GenerationRealtimeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = grq.Limit(1).IDs(newQueryContext(ctx, TypeGenerationRealtime, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{generationrealtime.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) FirstIDX(ctx context.Context) int {
	id, err := grq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GenerationRealtime entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GenerationRealtime entity is found.
// Returns a *NotFoundError when no GenerationRealtime entities are found.
func (grq *GenerationRealtimeQuery) Only(ctx context.Context) (*GenerationRealtime, error) {
	nodes, err := grq.Limit(2).All(newQueryContext(ctx, TypeGenerationRealtime, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{generationrealtime.Label}
	default:
		return nil, &NotSingularError{generationrealtime.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) OnlyX(ctx context.Context) *GenerationRealtime {
	node, err := grq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GenerationRealtime ID in the query.
// Returns a *NotSingularError when more than one GenerationRealtime ID is found.
// Returns a *NotFoundError when no entities are found.
func (grq *GenerationRealtimeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = grq.Limit(2).IDs(newQueryContext(ctx, TypeGenerationRealtime, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{generationrealtime.Label}
	default:
		err = &NotSingularError{generationrealtime.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) OnlyIDX(ctx context.Context) int {
	id, err := grq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GenerationRealtimes.
func (grq *GenerationRealtimeQuery) All(ctx context.Context) ([]*GenerationRealtime, error) {
	ctx = newQueryContext(ctx, TypeGenerationRealtime, "All")
	if err := grq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GenerationRealtime, *GenerationRealtimeQuery]()
	return withInterceptors[[]*GenerationRealtime](ctx, grq, qr, grq.inters)
}

// AllX is like All, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) AllX(ctx context.Context) []*GenerationRealtime {
	nodes, err := grq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GenerationRealtime IDs.
func (grq *GenerationRealtimeQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	ctx = newQueryContext(ctx, TypeGenerationRealtime, "IDs")
	if err := grq.Select(generationrealtime.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) IDsX(ctx context.Context) []int {
	ids, err := grq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (grq *GenerationRealtimeQuery) Count(ctx context.Context) (int, error) {
	ctx = newQueryContext(ctx, TypeGenerationRealtime, "Count")
	if err := grq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, grq, querierCount[*GenerationRealtimeQuery](), grq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) CountX(ctx context.Context) int {
	count, err := grq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (grq *GenerationRealtimeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = newQueryContext(ctx, TypeGenerationRealtime, "Exist")
	switch _, err := grq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (grq *GenerationRealtimeQuery) ExistX(ctx context.Context) bool {
	exist, err := grq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GenerationRealtimeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (grq *GenerationRealtimeQuery) Clone() *GenerationRealtimeQuery {
	if grq == nil {
		return nil
	}
	return &GenerationRealtimeQuery{
		config:     grq.config,
		limit:      grq.limit,
		offset:     grq.offset,
		order:      append([]OrderFunc{}, grq.order...),
		inters:     append([]Interceptor{}, grq.inters...),
		predicates: append([]predicate.GenerationRealtime{}, grq.predicates...),
		// clone intermediate query.
		sql:    grq.sql.Clone(),
		path:   grq.path,
		unique: grq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (grq *GenerationRealtimeQuery) GroupBy(field string, fields ...string) *GenerationRealtimeGroupBy {
	grq.fields = append([]string{field}, fields...)
	grbuild := &GenerationRealtimeGroupBy{build: grq}
	grbuild.flds = &grq.fields
	grbuild.label = generationrealtime.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (grq *GenerationRealtimeQuery) Select(fields ...string) *GenerationRealtimeSelect {
	grq.fields = append(grq.fields, fields...)
	sbuild := &GenerationRealtimeSelect{GenerationRealtimeQuery: grq}
	sbuild.label = generationrealtime.Label
	sbuild.flds, sbuild.scan = &grq.fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GenerationRealtimeSelect configured with the given aggregations.
func (grq *GenerationRealtimeQuery) Aggregate(fns ...AggregateFunc) *GenerationRealtimeSelect {
	return grq.Select().Aggregate(fns...)
}

func (grq *GenerationRealtimeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range grq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, grq); err != nil {
				return err
			}
		}
	}
	for _, f := range grq.fields {
		if !generationrealtime.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if grq.path != nil {
		prev, err := grq.path(ctx)
		if err != nil {
			return err
		}
		grq.sql = prev
	}
	return nil
}

func (grq *GenerationRealtimeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GenerationRealtime, error) {
	var (
		nodes = []*GenerationRealtime{}
		_spec = grq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GenerationRealtime).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GenerationRealtime{config: grq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, grq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (grq *GenerationRealtimeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := grq.querySpec()
	_spec.Node.Columns = grq.fields
	if len(grq.fields) > 0 {
		_spec.Unique = grq.unique != nil && *grq.unique
	}
	return sqlgraph.CountNodes(ctx, grq.driver, _spec)
}

func (grq *GenerationRealtimeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   generationrealtime.Table,
			Columns: generationrealtime.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: generationrealtime.FieldID,
			},
		},
		From:   grq.sql,
		Unique: true,
	}
	if unique := grq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := grq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, generationrealtime.FieldID)
		for i := range fields {
			if fields[i] != generationrealtime.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := grq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := grq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := grq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := grq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (grq *GenerationRealtimeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(grq.driver.Dialect())
	t1 := builder.Table(generationrealtime.Table)
	columns := grq.fields
	if len(columns) == 0 {
		columns = generationrealtime.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if grq.sql != nil {
		selector = grq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if grq.unique != nil && *grq.unique {
		selector.Distinct()
	}
	for _, p := range grq.predicates {
		p(selector)
	}
	for _, p := range grq.order {
		p(selector)
	}
	if offset := grq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := grq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GenerationRealtimeGroupBy is the group-by builder for GenerationRealtime entities.
type GenerationRealtimeGroupBy struct {
	selector
	build *GenerationRealtimeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (grgb *GenerationRealtimeGroupBy) Aggregate(fns ...AggregateFunc) *GenerationRealtimeGroupBy {
	grgb.fns = append(grgb.fns, fns...)
	return grgb
}

// Scan applies the selector query and scans the result into the given value.
func (grgb *GenerationRealtimeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeGenerationRealtime, "GroupBy")
	if err := grgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GenerationRealtimeQuery, *GenerationRealtimeGroupBy](ctx, grgb.build, grgb, grgb.build.inters, v)
}

func (grgb *GenerationRealtimeGroupBy) sqlScan(ctx context.Context, root *GenerationRealtimeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(grgb.fns))
	for _, fn := range grgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*grgb.flds)+len(grgb.fns))
		for _, f := range *grgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*grgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GenerationRealtimeSelect is the builder for selecting fields of GenerationRealtime entities.
type GenerationRealtimeSelect struct {
	*GenerationRealtimeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (grs *GenerationRealtimeSelect) Aggregate(fns ...AggregateFunc) *GenerationRealtimeSelect {
	grs.fns = append(grs.fns, fns...)
	return grs
}

// Scan applies the selector query and scans the result into the given value.
func (grs *GenerationRealtimeSelect) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeGenerationRealtime, "Select")
	if err := grs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GenerationRealtimeQuery, *GenerationRealtimeSelect](ctx, grs.GenerationRealtimeQuery, grs, grs.inters, v)
}

func (grs *GenerationRealtimeSelect) sqlScan(ctx context.Context, root *GenerationRealtimeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(grs.fns))
	for _, fn := range grs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*grs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
