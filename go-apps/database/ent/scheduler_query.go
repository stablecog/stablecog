// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yekta/stablecog/go-apps/database/ent/predicate"
	"github.com/yekta/stablecog/go-apps/database/ent/scheduler"
)

// SchedulerQuery is the builder for querying Scheduler entities.
type SchedulerQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	inters     []Interceptor
	predicates []predicate.Scheduler
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SchedulerQuery builder.
func (sq *SchedulerQuery) Where(ps ...predicate.Scheduler) *SchedulerQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *SchedulerQuery) Limit(limit int) *SchedulerQuery {
	sq.limit = &limit
	return sq
}

// Offset to start from.
func (sq *SchedulerQuery) Offset(offset int) *SchedulerQuery {
	sq.offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *SchedulerQuery) Unique(unique bool) *SchedulerQuery {
	sq.unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *SchedulerQuery) Order(o ...OrderFunc) *SchedulerQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// First returns the first Scheduler entity from the query.
// Returns a *NotFoundError when no Scheduler was found.
func (sq *SchedulerQuery) First(ctx context.Context) (*Scheduler, error) {
	nodes, err := sq.Limit(1).All(newQueryContext(ctx, TypeScheduler, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{scheduler.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *SchedulerQuery) FirstX(ctx context.Context) *Scheduler {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Scheduler ID from the query.
// Returns a *NotFoundError when no Scheduler ID was found.
func (sq *SchedulerQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(1).IDs(newQueryContext(ctx, TypeScheduler, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{scheduler.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *SchedulerQuery) FirstIDX(ctx context.Context) int {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Scheduler entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Scheduler entity is found.
// Returns a *NotFoundError when no Scheduler entities are found.
func (sq *SchedulerQuery) Only(ctx context.Context) (*Scheduler, error) {
	nodes, err := sq.Limit(2).All(newQueryContext(ctx, TypeScheduler, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{scheduler.Label}
	default:
		return nil, &NotSingularError{scheduler.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *SchedulerQuery) OnlyX(ctx context.Context) *Scheduler {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Scheduler ID in the query.
// Returns a *NotSingularError when more than one Scheduler ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *SchedulerQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(2).IDs(newQueryContext(ctx, TypeScheduler, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{scheduler.Label}
	default:
		err = &NotSingularError{scheduler.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *SchedulerQuery) OnlyIDX(ctx context.Context) int {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Schedulers.
func (sq *SchedulerQuery) All(ctx context.Context) ([]*Scheduler, error) {
	ctx = newQueryContext(ctx, TypeScheduler, "All")
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Scheduler, *SchedulerQuery]()
	return withInterceptors[[]*Scheduler](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *SchedulerQuery) AllX(ctx context.Context) []*Scheduler {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Scheduler IDs.
func (sq *SchedulerQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	ctx = newQueryContext(ctx, TypeScheduler, "IDs")
	if err := sq.Select(scheduler.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *SchedulerQuery) IDsX(ctx context.Context) []int {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *SchedulerQuery) Count(ctx context.Context) (int, error) {
	ctx = newQueryContext(ctx, TypeScheduler, "Count")
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*SchedulerQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *SchedulerQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *SchedulerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = newQueryContext(ctx, TypeScheduler, "Exist")
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *SchedulerQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SchedulerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *SchedulerQuery) Clone() *SchedulerQuery {
	if sq == nil {
		return nil
	}
	return &SchedulerQuery{
		config:     sq.config,
		limit:      sq.limit,
		offset:     sq.offset,
		order:      append([]OrderFunc{}, sq.order...),
		inters:     append([]Interceptor{}, sq.inters...),
		predicates: append([]predicate.Scheduler{}, sq.predicates...),
		// clone intermediate query.
		sql:    sq.sql.Clone(),
		path:   sq.path,
		unique: sq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (sq *SchedulerQuery) GroupBy(field string, fields ...string) *SchedulerGroupBy {
	sq.fields = append([]string{field}, fields...)
	grbuild := &SchedulerGroupBy{build: sq}
	grbuild.flds = &sq.fields
	grbuild.label = scheduler.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (sq *SchedulerQuery) Select(fields ...string) *SchedulerSelect {
	sq.fields = append(sq.fields, fields...)
	sbuild := &SchedulerSelect{SchedulerQuery: sq}
	sbuild.label = scheduler.Label
	sbuild.flds, sbuild.scan = &sq.fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SchedulerSelect configured with the given aggregations.
func (sq *SchedulerQuery) Aggregate(fns ...AggregateFunc) *SchedulerSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *SchedulerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.fields {
		if !scheduler.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *SchedulerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Scheduler, error) {
	var (
		nodes = []*Scheduler{}
		_spec = sq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Scheduler).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Scheduler{config: sq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sq *SchedulerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	_spec.Node.Columns = sq.fields
	if len(sq.fields) > 0 {
		_spec.Unique = sq.unique != nil && *sq.unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *SchedulerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   scheduler.Table,
			Columns: scheduler.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: scheduler.FieldID,
			},
		},
		From:   sq.sql,
		Unique: true,
	}
	if unique := sq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := sq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, scheduler.FieldID)
		for i := range fields {
			if fields[i] != scheduler.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *SchedulerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(scheduler.Table)
	columns := sq.fields
	if len(columns) == 0 {
		columns = scheduler.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.unique != nil && *sq.unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SchedulerGroupBy is the group-by builder for Scheduler entities.
type SchedulerGroupBy struct {
	selector
	build *SchedulerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *SchedulerGroupBy) Aggregate(fns ...AggregateFunc) *SchedulerGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *SchedulerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeScheduler, "GroupBy")
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SchedulerQuery, *SchedulerGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *SchedulerGroupBy) sqlScan(ctx context.Context, root *SchedulerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SchedulerSelect is the builder for selecting fields of Scheduler entities.
type SchedulerSelect struct {
	*SchedulerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *SchedulerSelect) Aggregate(fns ...AggregateFunc) *SchedulerSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *SchedulerSelect) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeScheduler, "Select")
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SchedulerQuery, *SchedulerSelect](ctx, ss.SchedulerQuery, ss, ss.inters, v)
}

func (ss *SchedulerSelect) sqlScan(ctx context.Context, root *SchedulerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
