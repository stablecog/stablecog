// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yekta/stablecog/go-apps/database/ent/negativeprompt"
	"github.com/yekta/stablecog/go-apps/database/ent/predicate"
)

// NegativePromptQuery is the builder for querying NegativePrompt entities.
type NegativePromptQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	inters     []Interceptor
	predicates []predicate.NegativePrompt
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NegativePromptQuery builder.
func (npq *NegativePromptQuery) Where(ps ...predicate.NegativePrompt) *NegativePromptQuery {
	npq.predicates = append(npq.predicates, ps...)
	return npq
}

// Limit the number of records to be returned by this query.
func (npq *NegativePromptQuery) Limit(limit int) *NegativePromptQuery {
	npq.limit = &limit
	return npq
}

// Offset to start from.
func (npq *NegativePromptQuery) Offset(offset int) *NegativePromptQuery {
	npq.offset = &offset
	return npq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (npq *NegativePromptQuery) Unique(unique bool) *NegativePromptQuery {
	npq.unique = &unique
	return npq
}

// Order specifies how the records should be ordered.
func (npq *NegativePromptQuery) Order(o ...OrderFunc) *NegativePromptQuery {
	npq.order = append(npq.order, o...)
	return npq
}

// First returns the first NegativePrompt entity from the query.
// Returns a *NotFoundError when no NegativePrompt was found.
func (npq *NegativePromptQuery) First(ctx context.Context) (*NegativePrompt, error) {
	nodes, err := npq.Limit(1).All(newQueryContext(ctx, TypeNegativePrompt, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{negativeprompt.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (npq *NegativePromptQuery) FirstX(ctx context.Context) *NegativePrompt {
	node, err := npq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NegativePrompt ID from the query.
// Returns a *NotFoundError when no NegativePrompt ID was found.
func (npq *NegativePromptQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = npq.Limit(1).IDs(newQueryContext(ctx, TypeNegativePrompt, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{negativeprompt.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (npq *NegativePromptQuery) FirstIDX(ctx context.Context) int {
	id, err := npq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NegativePrompt entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NegativePrompt entity is found.
// Returns a *NotFoundError when no NegativePrompt entities are found.
func (npq *NegativePromptQuery) Only(ctx context.Context) (*NegativePrompt, error) {
	nodes, err := npq.Limit(2).All(newQueryContext(ctx, TypeNegativePrompt, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{negativeprompt.Label}
	default:
		return nil, &NotSingularError{negativeprompt.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (npq *NegativePromptQuery) OnlyX(ctx context.Context) *NegativePrompt {
	node, err := npq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NegativePrompt ID in the query.
// Returns a *NotSingularError when more than one NegativePrompt ID is found.
// Returns a *NotFoundError when no entities are found.
func (npq *NegativePromptQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = npq.Limit(2).IDs(newQueryContext(ctx, TypeNegativePrompt, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{negativeprompt.Label}
	default:
		err = &NotSingularError{negativeprompt.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (npq *NegativePromptQuery) OnlyIDX(ctx context.Context) int {
	id, err := npq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NegativePrompts.
func (npq *NegativePromptQuery) All(ctx context.Context) ([]*NegativePrompt, error) {
	ctx = newQueryContext(ctx, TypeNegativePrompt, "All")
	if err := npq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NegativePrompt, *NegativePromptQuery]()
	return withInterceptors[[]*NegativePrompt](ctx, npq, qr, npq.inters)
}

// AllX is like All, but panics if an error occurs.
func (npq *NegativePromptQuery) AllX(ctx context.Context) []*NegativePrompt {
	nodes, err := npq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NegativePrompt IDs.
func (npq *NegativePromptQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	ctx = newQueryContext(ctx, TypeNegativePrompt, "IDs")
	if err := npq.Select(negativeprompt.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (npq *NegativePromptQuery) IDsX(ctx context.Context) []int {
	ids, err := npq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (npq *NegativePromptQuery) Count(ctx context.Context) (int, error) {
	ctx = newQueryContext(ctx, TypeNegativePrompt, "Count")
	if err := npq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, npq, querierCount[*NegativePromptQuery](), npq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (npq *NegativePromptQuery) CountX(ctx context.Context) int {
	count, err := npq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (npq *NegativePromptQuery) Exist(ctx context.Context) (bool, error) {
	ctx = newQueryContext(ctx, TypeNegativePrompt, "Exist")
	switch _, err := npq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (npq *NegativePromptQuery) ExistX(ctx context.Context) bool {
	exist, err := npq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NegativePromptQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (npq *NegativePromptQuery) Clone() *NegativePromptQuery {
	if npq == nil {
		return nil
	}
	return &NegativePromptQuery{
		config:     npq.config,
		limit:      npq.limit,
		offset:     npq.offset,
		order:      append([]OrderFunc{}, npq.order...),
		inters:     append([]Interceptor{}, npq.inters...),
		predicates: append([]predicate.NegativePrompt{}, npq.predicates...),
		// clone intermediate query.
		sql:    npq.sql.Clone(),
		path:   npq.path,
		unique: npq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (npq *NegativePromptQuery) GroupBy(field string, fields ...string) *NegativePromptGroupBy {
	npq.fields = append([]string{field}, fields...)
	grbuild := &NegativePromptGroupBy{build: npq}
	grbuild.flds = &npq.fields
	grbuild.label = negativeprompt.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (npq *NegativePromptQuery) Select(fields ...string) *NegativePromptSelect {
	npq.fields = append(npq.fields, fields...)
	sbuild := &NegativePromptSelect{NegativePromptQuery: npq}
	sbuild.label = negativeprompt.Label
	sbuild.flds, sbuild.scan = &npq.fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NegativePromptSelect configured with the given aggregations.
func (npq *NegativePromptQuery) Aggregate(fns ...AggregateFunc) *NegativePromptSelect {
	return npq.Select().Aggregate(fns...)
}

func (npq *NegativePromptQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range npq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, npq); err != nil {
				return err
			}
		}
	}
	for _, f := range npq.fields {
		if !negativeprompt.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if npq.path != nil {
		prev, err := npq.path(ctx)
		if err != nil {
			return err
		}
		npq.sql = prev
	}
	return nil
}

func (npq *NegativePromptQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NegativePrompt, error) {
	var (
		nodes = []*NegativePrompt{}
		_spec = npq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NegativePrompt).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NegativePrompt{config: npq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, npq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (npq *NegativePromptQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := npq.querySpec()
	_spec.Node.Columns = npq.fields
	if len(npq.fields) > 0 {
		_spec.Unique = npq.unique != nil && *npq.unique
	}
	return sqlgraph.CountNodes(ctx, npq.driver, _spec)
}

func (npq *NegativePromptQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   negativeprompt.Table,
			Columns: negativeprompt.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: negativeprompt.FieldID,
			},
		},
		From:   npq.sql,
		Unique: true,
	}
	if unique := npq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := npq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, negativeprompt.FieldID)
		for i := range fields {
			if fields[i] != negativeprompt.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := npq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := npq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := npq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := npq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (npq *NegativePromptQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(npq.driver.Dialect())
	t1 := builder.Table(negativeprompt.Table)
	columns := npq.fields
	if len(columns) == 0 {
		columns = negativeprompt.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if npq.sql != nil {
		selector = npq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if npq.unique != nil && *npq.unique {
		selector.Distinct()
	}
	for _, p := range npq.predicates {
		p(selector)
	}
	for _, p := range npq.order {
		p(selector)
	}
	if offset := npq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := npq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NegativePromptGroupBy is the group-by builder for NegativePrompt entities.
type NegativePromptGroupBy struct {
	selector
	build *NegativePromptQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (npgb *NegativePromptGroupBy) Aggregate(fns ...AggregateFunc) *NegativePromptGroupBy {
	npgb.fns = append(npgb.fns, fns...)
	return npgb
}

// Scan applies the selector query and scans the result into the given value.
func (npgb *NegativePromptGroupBy) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeNegativePrompt, "GroupBy")
	if err := npgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NegativePromptQuery, *NegativePromptGroupBy](ctx, npgb.build, npgb, npgb.build.inters, v)
}

func (npgb *NegativePromptGroupBy) sqlScan(ctx context.Context, root *NegativePromptQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(npgb.fns))
	for _, fn := range npgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*npgb.flds)+len(npgb.fns))
		for _, f := range *npgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*npgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := npgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NegativePromptSelect is the builder for selecting fields of NegativePrompt entities.
type NegativePromptSelect struct {
	*NegativePromptQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nps *NegativePromptSelect) Aggregate(fns ...AggregateFunc) *NegativePromptSelect {
	nps.fns = append(nps.fns, fns...)
	return nps
}

// Scan applies the selector query and scans the result into the given value.
func (nps *NegativePromptSelect) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeNegativePrompt, "Select")
	if err := nps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NegativePromptQuery, *NegativePromptSelect](ctx, nps.NegativePromptQuery, nps, nps.inters, v)
}

func (nps *NegativePromptSelect) sqlScan(ctx context.Context, root *NegativePromptQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(nps.fns))
	for _, fn := range nps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*nps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
